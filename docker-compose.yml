version: '3.8'
services:
  app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - sqlite-data:/app/data
    command: npm run dev
    environment:
      - NODE_ENV=development

volumes:
  sqlite-data:
